// Spinner.js
import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      {/* You can customize this with any spinner/loading animation */}
      <div className="loader">Loading...</div>
    </div>
  );
};

export default Spinner;
